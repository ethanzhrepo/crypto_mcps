# Test Environment Configuration
# For unit tests: use mock keys (test-key)
# For live integration tests: replace with real API keys

# ==================== Test Mode ====================
# mock: Unit tests with mocked responses
# live: Integration tests with real API calls
TEST_MODE=mock

# ==================== Server Configuration ====================
MCP_SERVER_HOST=0.0.0.0
MCP_SERVER_PORT=8000
LOG_LEVEL=WARNING
ENVIRONMENT=test

# ==================== Redis Configuration ====================
REDIS_URL=redis://localhost:6379/0
REDIS_MAX_CONNECTIONS=5

# ==================== Telegram Scraper ====================
# Telegram 搜索工具依赖的 scraper 服务地址（通常是一个 Elasticsearch proxy / API 服务）
TELEGRAM_SCRAPER_URL=http://localhost:8000
# 目前对接 tel2es FastAPI（/search, /latest），索引名由 tel2es 服务端配置管理，无需客户端配置

# ==================== API Keys ====================
# For live tests, replace 'test-key' with real API keys
# Keys marked [FREE] work without authentication for basic usage

# Price & Market Data
COINGECKO_API_KEY=test-key                    # [FREE] Basic tier works without key
COINMARKETCAP_API_KEY=test-key
ETHERSCAN_API_KEY=test-key
BSCSCAN_API_KEY=test-key
BASESCAN_API_KEY=test-key
POLYGONSCAN_API_KEY=test-key
ARBISCAN_API_KEY=test-key
GITHUB_TOKEN=test-token
MESSARI_API_KEY=test-key
CRYPTOPANIC_API_KEY=test-key
BRAVE_SEARCH_API_KEY=test-key
FRED_API_KEY=test-key

# Search Engines
GOOGLE_SEARCH_API_KEY=test-key
GOOGLE_CSE_ID=test-cse-id
BING_SEARCH_API_KEY=test-key
SERPAPI_KEY=test-key
KAITO_API_KEY=test-key

# Derivatives & Onchain Data
COINGLASS_API_KEY=test-key
WHALE_ALERT_API_KEY=test-key
TOKEN_UNLOCKS_API_KEY=test-key
GOPLUS_API_KEY=test-key
GOPLUS_API_SECRET=test-secret
TALLY_API_KEY=test-key
XAI_API_KEY=test-key

# The Graph (DEX Liquidity Data)
# Free tier: 100,000 queries/month
# Get API key: https://thegraph.com/studio/
THEGRAPH_API_KEY=test-key

# Contract Risk Analysis Provider
CONTRACT_RISK_PROVIDER=goplus

# ==================== Rate Limits ====================
MAX_CONCURRENT_REQUESTS=10
RATE_LIMIT_COINGECKO=50
RATE_LIMIT_COINMARKETCAP=30
RATE_LIMIT_ETHERSCAN=5
RATE_LIMIT_GITHUB=60

# ==================== Timeouts ====================
DEFAULT_REQUEST_TIMEOUT=10.0
SLOW_REQUEST_TIMEOUT=30.0

# ==================== Feature Flags ====================
ENABLE_CACHE=true
ENABLE_FALLBACK=true
ENABLE_METRICS=false
